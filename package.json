{
  "name": "typorm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start:postgres": "docker-compose up -d",
    "stop:postgres": "docker rm $(docker stop $(docker ps -a -q --filter ancestor=postgres:13))"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typeorm": "^0.2.37",
    "typescript": "^4.3.5",
    "pg": "^8.7.1"
  }
}
